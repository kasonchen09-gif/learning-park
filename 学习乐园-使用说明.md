# 🌈 学习乐园 - 使用说明

## 快速开始

1. 用浏览器直接打开 `learning-park.html` 即可使用
2. 手机/iPad 也可直接打开，已完美适配移动端
3. 所有数据存储在浏览器 localStorage 中，不清除缓存就不会丢失
4. 无需任何服务器、无需安装任何依赖

## 项目结构（单文件内部分节）

| Section | 内容 | 说明 |
|---------|------|------|
| CSS (~200行) | 马卡龙色系变量、响应式布局、动画 | 修改配色改 `:root` 变量 |
| HTML (~100行) | DOM结构：3层导航 + 5个浮层 | 添加新页面在此扩展 |
| Utils & Config (~80行) | 几何工具函数、学科/关卡配置 | **扩展新单元主要改这里** |
| Storage (~50行) | localStorage读写、每日重置 | 数据结构在 DEFAULT_STATE |
| SoundFX (~60行) | Web Audio API音效 | 无需外部音频文件 |
| Canvas Engine (~100行) | Vertex、Polygon、Parallelogram等类 | 添加新关卡形状在此扩展 |
| DragManager (~70行) | Pointer Events拖拽管理 | 通用组件，通常无需修改 |
| LevelManager (~250行) | 6个关卡的setup+render+交互 | **添加新关卡模板在此** |
| Confetti (~60行) | Canvas粒子动画 | 调用 `confetti.burst(N)` |
| UI Controller (~400行) | 导航、进度、宠物、任务、弹窗 | 核心业务逻辑 |
| App Init (~100行) | 事件代理、初始化 | 启动入口 |

## 如何扩展新单元

### 1. 添加单元配置

在 `CONFIG.math.units` 数组中添加新单元：

```javascript
{
  id: 'decimal-multiply',       // 唯一ID（kebab-case）
  name: '小数乘法',              // 显示名称
  icon: '✖️',                   // 图标emoji
  totalLevels: 5,               // 关卡总数
  knowledgePoints: [            // 知识点列表（与关卡对应）
    '小数乘整数 = 整数乘法 + 小数点移位',
    '小数乘小数的计算方法',
    // ...
  ],
  levels: [                     // 关卡定义
    {
      id: 1,
      title: '小数乘整数的意义',
      desc: '理解小数乘整数的含义',
      hint: '💡 操作提示文字'
    },
    // ... 更多关卡
  ]
}
```

### 2. 在 LevelManager 中添加关卡逻辑

在 `_render()` 方法的 switch 中增加 case，在 `loadLevel()` 的 switch 中增加对应 setup：

```javascript
// 在 _render() 中：
case 7: this._renderLevel7(ctx); break;

// 添加 setup 方法：
_setupLevel7() {
  // 创建图形、设置交互...
}

// 添加 render 方法：
_renderLevel7(ctx) {
  // 每帧绘制...
}
```

### 3. 在 UIController 中添加关卡完成逻辑

```javascript
_handleLevel7Complete() {
  // 验证用户操作，调用 this._completeLevel(stars, msg, formula)
}
```

并在 `_updateGameButtons()` 中添加 case 7 的按钮处理。

### 4. 关卡模板代码（复制以下结构）

```javascript
// === LEVEL N: 关卡名称 ===
_setupLevelN() {
  // 创建顶点（固定顶点 color: '#999'，可拖拽顶点 color: '#4CAF50'）
  const v1 = new Vertex(x, y, { constraints: {...}, color: '#4CAF50', label: '拖' });
  const v2 = new Vertex(x, y, { color: '#999' });
  // ...更多顶点
  
  // 创建形状（根据关卡选择合适类：Polygon/Parallelogram/TriangleShape/TrapezoidShape）
  const shape = new Polygon([v1, v2, ...], {
    fillColor: 'rgba(r,g,b,0.3)', strokeColor: '#color', lineWidth: 3,
    label: '标签', id: 'shape-id', draggable: false
  });
  
  this.shapes = [shape];
  this._shape = shape;  // 保存引用
}

_renderLevelN(ctx) {
  // 绘制辅助线、标注
  this._drawDimLine(ctx, x1, y1, x2, y2, '标注文字', '#color');
  // 显示提示面板
  this._drawPanel(ctx, '提示文字', x, y, '#color');
  
  // 根据 phase 显示不同内容
  if (this.phase === 'idle') { ... }
  if (this.phase === 'snapped') { ... }
}

// 如果需要自定义交互，在 handleCanvasClick / handleDragEnd 中添加对应 case
```

## 自定义样式

所有颜色在 CSS `:root` 变量中统一定义：

```css
--pink: #FFB3BA;
--peach: #FFDFBA;
--yellow: #FFFFBA;
--mint: #BAFFC9;
--sky: #BAE1FF;
--lavender: #D4BAFF;
```

修改这些变量即可全局换色。

## 常见问题

**Q: 如何重置全部数据？**
A: 点击右上角 ⚙️ 设置 → 底部「重置全部数据」

**Q: localStorage 数据在哪里？**
A: 浏览器 DevTools → Application → Local Storage → 键名 `learning_park_data`

**Q: 如何调试关卡Canvas？**
A: 浏览器打开 DevTools，在 LevelManager 的 `_render()` 中设断点

**Q: 音效不响？**
A: 浏览器需用户交互一次后才能播放音频（安全策略）。点击页面任意位置即可激活。

## 技术特点

- **零依赖**：单文件HTML，无任何外部CSS/JS/字体/图片引用
- **Canvas交互**：几何图形拖拽、切割、拼接全部用Canvas实现
- **Web Audio音效**：用OscillatorNode生成，无需音频文件
- **localStorage持久化**：进度、星星、宠物状态全保存在本地
- **移动端优先**：触摸目标≥44px，适配iPhone/iPad横竖屏
- **响应式**：375px~1200px+ 全覆盖
